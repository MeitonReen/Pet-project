<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    >
	<Style x:Key="BindingFligthsSchedule">
		<Setter 
				Property="ListBox.DataContext"
				Value="{DynamicResource DCCreatingOrder}">
		</Setter>
	
		<Setter
				Property="ListBox.ItemsSource"
				Value="{Binding GettedFlightsSchedule, Mode = TwoWay}"
		></Setter>
		<Setter
				Property="ListBox.ItemTemplate">
			<Setter.Value>
				<DataTemplate>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="auto"></ColumnDefinition>
							<ColumnDefinition Width="*"></ColumnDefinition>
						</Grid.ColumnDefinitions>

						<RadioButton
							Grid.Column="0"
							GroupName="FligthShedule"
							IsChecked="{Binding IsSelected, Mode = TwoWay}"
							HorizontalAlignment="Center"
							VerticalAlignment="Center"
						></RadioButton>
						<TextBlock
							Grid.Column="1"
							Text="{Binding DateTimeOfFlight}"
						></TextBlock>
					</Grid>
				</DataTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>